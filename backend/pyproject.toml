[project]
name = "backend"
version = "0.1.0"
description = "Sunstead FYP backend — DID-based OSS profile and feed"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.138.0",
    "httpx>=0.27.0",
    "numpy>=2.0.0",
    "uvicorn[standard]>=0.32.0",
]
